<script lang="ts">
	/**
	 * Character Card layout.
	 * Photo with ability to change
	 * Name and class
	 * Stats and modifiers
	 * feats
	 * notes ( for now will also be inventory)
	 * Dice roll button ( will link to another lib file)
	 *
	 */

	import { invoke } from '@tauri-apps/api/tauri';

	let name: string = '';
	let characterClass: string = '';
</script>
